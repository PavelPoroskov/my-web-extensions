font
  label has different view for 
    https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/API/webNavigation
    https://developer.chrome.com/docs/extensions/get-started/tutorial/hello-world 

    use fixed font-family sans-serif

  i use fixed font size in pixels
  TODO  may be use other units for responsive

  TODO user select in options: font size, font color, background-color

DONE? cache (url to bookmark folder) is not required?
  It complicated code.
  Do not give optimization?

DONE position: fixed

DONE cache last 100 results

DONE start search folder on opening link


async function closeBookmarkedTabs11() {
  const tabs = await chrome.tabs.query({ lastFocusedWindow: true });
  const tabsWithId = tabs.filter(({ id }) => id);
  
  const {
    tabWithBookmarkIdList,
  } = await getTabsWithBookmark(tabsWithId);

  if (tabWithBookmarkIdList.length > 0) {
    await chrome.tabs.remove(tabWithBookmarkIdList);
  }
}
